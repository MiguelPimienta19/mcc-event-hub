/**
 * API Configuration Constants
 *
 * Centralized API URL configuration to avoid duplication across the codebase.
 */

export const API_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
